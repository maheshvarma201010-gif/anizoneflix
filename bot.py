from bot import bot

# This file is kept for backward compatibility if needed,
# but the bot is now started via FastAPI lifecycle in app.py.

if __name__ == "__main__":
    print("Please run main.py to start both the Web Server and the Bot.")
